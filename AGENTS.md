# Agent Instructions

## Git Workflow

- Automatically stage, commit with a clear and descriptive message, and push changes to GitHub (`git push origin main`) after completing code modifications or user requests.
