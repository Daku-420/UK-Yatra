import { TourPackage } from '../types';

/**
 * Tour Packages & Itineraries Catalog
 * Currently empty - all packages and day-wise itineraries will be added directly
 * by the user through the Admin Portal (/admin) or custom creation.
 */
export const TOUR_PACKAGES: TourPackage[] = [];
